import { COLORS } from '../constants';

export default function TheStream() {
  return (
    <div className="flow-placeholder" style={{ color: COLORS.textDim }}>
      THE STREAM — Phase 2
    </div>
  );
}
