# NPM authentication for @megaeth-labs private packages
# Token configured as NPM_TOKEN env var in Render
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
   @megaeth-labs:registry=https://registry.npmjs.org/
